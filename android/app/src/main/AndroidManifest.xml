<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.finance.app">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- OCR 拍照 -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <!-- 语音记账：麦克风（speech_to_text 运行时会自行弹权限请求） -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- 本地推送桥接：Android 13+ 通知运行时权限（flutter_local_notifications
         在 Dart 侧请求；用户拒绝则静默跳过本地推送，通知中心照常可用）。
         只用 show() 即时通知，不涉及定时/闹钟，故无需 SCHEDULE_EXACT_ALARM
         等额外权限与 receiver。 -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:label="司库"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--
          端侧自动记账：监听支付 / 银行 App 通知。
          需在系统「通知使用权」设置页由用户手动授权后才生效；
          解析全程在本机完成，不上传通知原文。
        -->
        <service
            android:name=".AutoBookkeepingListenerService"
            android:label="司库 · 自动记账"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <!-- url_launcher：Android 11+ 需声明能打开 https 链接（财经资讯原文外链） -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="http" />
        </intent>
        <!-- 语音记账：Android 11+ 包可见性，用于探测系统语音识别服务是否可用 -->
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
    </queries>
</manifest>
