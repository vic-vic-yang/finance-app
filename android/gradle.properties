org.gradle.jvmargs=-Xmx4G
android.useAndroidX=true
android.enableJetifier=true

# Kotlin 增量编译在 Windows + 工程/pub cache 跨盘符（D:\ vs C:\）时会触发
# RelocatableFileToPathConverter 的 IllegalArgumentException，
# 导致 daemon 报错回退非增量编译。彻底关增量，避免噪音 + 加快首次成功率。
kotlin.incremental=false
